<div class="page-header">
    <h1>Details of cpost</h1>
</div>


<table class="table table-bordered">
    <tbody>
        <tr><th>title</th><td><%= cpost.title %></td></tr>
        <tr><th>content</th><td><%= cpost.content %></td></tr>
        <tr><th>createdAt</th><td><%= cpost.createdAt %></td></tr>    
    </tbody>
</table>

<div class="well">
    <%- link_to('<i class="icon-edit icon-white"></i> Edit', path_to.edit_cpost(cpost), {class: 'btn btn-primary'}) %>
    <%- link_to('<i class="icon-remove icon-white"></i> Delete', path_to.cpost(cpost), {class: 'btn btn-danger', method: 'delete', remote: true, jsonp: '(function (u) { location.href = u; })'}) %>
    or
    <%- link_to('Back to index', path_to.cposts()) %>
</div>